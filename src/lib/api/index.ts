// Export the main API client and its services
export { default as apiClient, ApiClient } from './client.api';

// Export service classes for custom instantiation if needed
export { AuthService } from './service/auth.service';
export { CommandsService } from './service/commands.service';
